namespace DynamicFormValidator.Presentation;

public enum FormDataValidationType
{
    LESS_THAN,
    EQUALS,
    GREATER_THAN,
    LESS_THAN_OR_EQUALS,
    GREATER_THAN_OR_EQUALS,
    NOT_EQUALS,
    REGEX,
    REQUIRED
}